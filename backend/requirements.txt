# =====================================================================
# InsightHub Backend - Python Dependencies (نسخة Hugging Face Spaces)
#
# تغييرات مهمة عن النسخة الأولى:
# - حُذِفت Celery و Redis و Flower (استبدلت بـ FastAPI BackgroundTasks)
# - الـ pipeline يعمل داخل نفس process الـ FastAPI مع تحكم بالتوازي
# =====================================================================

# ----- Web framework -----
fastapi==0.111.0
uvicorn[standard]==0.30.0
python-dotenv==1.0.1
pydantic==2.7.1
pydantic-settings==2.2.1

# ----- Supabase / HTTP -----
supabase==2.4.2
postgrest==0.16.8
httpx==0.27.0

# ----- Big Data: PySpark -----
pyspark==3.5.1
pyarrow==16.1.0

# ----- Datasets streaming -----
datasets==2.19.1
huggingface-hub==0.23.0
fsspec==2024.3.1

# ----- NLP / ML -----
transformers==4.41.0
torch==2.3.0
sentencepiece==0.2.0
scikit-learn==1.5.0
nltk==3.8.1

# Topic modeling (BERTopic اختياري - حذفناه لتسريع البناء على HF)
# bertopic==0.16.2
# umap-learn==0.5.6
# hdbscan==0.8.36

# ----- Network -----
networkx==3.3

# ----- Data manipulation -----
pandas==2.2.2
numpy==1.26.4

# ----- Utilities -----
loguru==0.7.2
tenacity==8.3.0
python-dateutil==2.9.0
